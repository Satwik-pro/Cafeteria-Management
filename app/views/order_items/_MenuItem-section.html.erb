<% MenuItem.order(:menu_category_id).each do |menu| %>
<div class="Dish-Card">
  <%= form_with(url: "/order_items", method: "post") do %>
  <img class="Dish" src=<%= menu.description %>/>
  <div class="Dish-Name"><%= menu.name %></div>
  <div class="Dish-Price">Price: ₹ <%= menu.price %></div>
  <%= button_tag("Add To Orders", type: "submit", class: "Add-To-Orders", :value => menu.menu_category_id, :name => "id") %>
  <% end %>
</div>
<% end %>
